1:100
employees <- paste(number)
# Create a vector of 100 employees ("Employee 1", "Employee 2", ... "Employee 100")
# Hint: use the `paste()` function and vector recycling to add a number to the word
# "Employee"
number <- 1:100
employees <- paste(number)
employees
number <- 1:100
employees <- paste("employee"number)
employees <- paste("employee"number)
employees <- "employee"paste(number)
employees <- "employee"paste (number)
employees <- paste("employee"number)
employees <- paste0("employee", number)
employees
employees <- paste("employee", number)
employees
runif(40000:50000)
runif(40000)
runif
?runif
runif(100, min=40000, max= 50000)
# Create a vector of 100 random salaries for the year 2017
# Use the `runif()` function to pick random numbers between 40000 and 50000
salary <- runif(100, min=40000, max= 50000)
salary
# Create a vector of 100 random salaries for the year 2017
# Use the `runif()` function to pick random numbers between 40000 and 50000
salary_2017 <- runif(100, min=40000, max= 50000)
# Create a vector of 100 salaries in 2018 that have changed from 2017
# Use `runif()` to add a random number between -5000 and 10000 to each of 2017's
# salaries (the negative number means that a salary may have decreased!)
salary_2018 <- salary_2017+runif(100, min= -5000, max= 10000)
salary_2018
# Create a vector of 100 salaries in 2018 that have changed from 2017
# Use `runif()` to add a random number between -5000 and 10000 to each of 2017's
# salaries (the negative number means that a salary may have decreased!)
salary_2018 <- salary_2017+runif(100, min= 10000, max= 10000)
salary_2018
2000+-5000
a <- (200,300)
a <- c(200,300)
b <- a+20\
b <- a+20
b
b <- a + runif(2, min= -5000, max= 10000)
b
runif(2, min= -5000, max= 10000)
runif(2, min= -5000, max= 10000)
runif(2, min= -5000, max= 10000)
number <- 1:100
employees <- paste("employee", number)
# Create a vector of 100 random salaries for the year 2017
# Use the `runif()` function to pick random numbers between 40000 and 50000
salary_2017 <- runif(100, min=40000, max= 50000)
# Create a vector of 100 salaries in 2018 that have changed from 2017
# Use `runif()` to add a random number between -5000 and 10000 to each of 2017's
# salaries (the negative number means that a salary may have decreased!)
salary_2018 <- salary_2017+runif(100, min= -5000, max= 10000)
salary_2018
salary
# Create a data frame 'salaries' by combining the 3 vectors you just made
# Remember to set `stringsAsFactors=FALSE`!
salaries <- data.frame(employees, salary_2017, salary_2018)
salaries
# Create a column 'change' that stores each person's change in salary between
# 2017 and 2018
salaries$change <- salary_2017- salary_2018
salaries
# Create a column 'got_raise' that is TRUE if the person got a raise (their
# salary went up)
salaries$got_raise <- paste("TRUE" if change>0)
# Create a column 'got_raise' that is TRUE if the person got a raise (their
# salary went up)
salaries$got_raise <- paste("TRUE") if change > 0
# Create a column 'got_raise' that is TRUE if the person got a raise (their
# salary went up)
salaries$got_raise <- "TRUE", if change > 0
# Create a column 'got_raise' that is TRUE if the person got a raise (their
# salary went up)
salaries$got_raise <- "TRUE" if change > 0
# Create a column 'got_raise' that is TRUE if the person got a raise (their
# salary went up)
salaries$got_raise <- "TRUE" [if change > 0]
# Create a column 'got_raise' that is TRUE if the person got a raise (their
# salary went up)
salaries$got_raise <- "TRUE" [if salaries$change > 0]
# Create a column 'got_raise' that is TRUE if the person got a raise (their
# salary went up)
salaries$got_raise <- "TRUE" {if salaries$change > 0}
# Create a column 'got_raise' that is TRUE if the person got a raise (their
# salary went up)
salaries$got_raise <- "TRUE" {
if salaries$change > 0
}
# Create a column 'got_raise' that is TRUE if the person got a raise (their
# salary went up)
salaries$got_raise <- "TRUE"
salaries
salaries$got_raise[df$change > 0] <- "TRUE"
# Create a column 'got_raise' that is TRUE if the person got a raise (their
# salary went up)
salaries$got_raise[salaries$change > 0] <- "TRUE"
salaries$got_raise[salaries$change > 0] <- "TRUE"
salaries$got_raise[salaries$change<=0] <- "FALSE"
salaries
View(salaries)
